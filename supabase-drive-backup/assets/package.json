{
  "name": "project-backup",
  "private": true,
  "type": "module",
  "description": "Daily Supabase → Google Drive backup (Excel + pg_dump). Isolated from the app deps.",
  "scripts": {
    "backup": "node backup.mjs",
    "get-token": "node get-refresh-token.mjs"
  },
  "dependencies": {
    "exceljs": "^4.4.0",
    "pg": "^8.13.1",
    "pg-cursor": "^2.12.1"
  }
}
